/* 简约主题：浅灰底、低饱和绿牌桌、白色卡片、墨色文字，全站只有一种强调色（品牌绿），
   没有金色/铜色点缀。变量只在 page 上定义一次（与 web 端 apps/web/static/styles.css
   同一套色值，改色要两端一起改），页面样式只做局部覆盖。 */
page {
  --bg: #F4F6F5;
  --surface: #FFFFFF;
  --surface-2: #EDF3EF;
  --line: #D9E3DD;
  --line-strong: #CDDDD3;
  --ink: #243C32;
  --muted: #64756D;
  --accent: #28694F;
  --accent-dark: #256B50;
  --accent-soft: #F0F7F2;
  --on-accent: #FFFFFF;
  /* 牌桌毡面：比 --surface-2 稍深一点，让椭圆桌面从浅灰底上浮出来。 */
  --felt: #E5EEE9;
  /* 牌背：低饱和的深绿，和毡面拉开层次又不抢牌面。与 web --card-back 同值。 */
  --card-back: #678778;
  --danger: #B3261E;
  --ok: #287455;

  background: var(--bg);
  color: var(--ink);
  font-size: 28rpx;
  line-height: 1.5;
}

.page {
  min-height: 100vh;
  padding: 24rpx;
  box-sizing: border-box;
}

.card {
  background: var(--surface);
  border: 1rpx solid var(--line);
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
}

.card-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--ink);
  margin-bottom: 12rpx;
}

.btn {
  display: block;
  width: 100%;
  padding: 24rpx 0;
  margin-top: 16rpx;
  border-radius: 12rpx;
  border: 1rpx solid var(--line-strong);
  background: var(--surface-2);
  color: var(--ink);
  font-size: 30rpx;
  text-align: center;
  box-sizing: border-box;
}

.btn::after {
  border: none;
}

.btn-primary {
  background: var(--accent);
  border-color: var(--accent);
  color: var(--on-accent);
  font-weight: 600;
}

.btn[disabled] {
  opacity: 0.45;
}

.row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.muted {
  color: var(--muted);
  font-size: 24rpx;
}

/* 强调文字：金额、准备状态、核验结果这类要抢眼的地方，一律走品牌绿。 */
.accent {
  color: var(--accent);
}

.danger {
  color: var(--danger);
}

.tag {
  display: inline-block;
  padding: 2rpx 12rpx;
  border-radius: 999rpx;
  border: 1rpx solid var(--line-strong);
  color: var(--muted);
  font-size: 22rpx;
}

.input {
  background: var(--surface-2);
  border: 1rpx solid var(--line);
  border-radius: 12rpx;
  padding: 18rpx;
  color: var(--ink);
}

.notice {
  background: var(--accent-soft);
  border-left: 6rpx solid var(--accent);
  border-radius: 8rpx;
  padding: 16rpx;
  font-size: 24rpx;
  margin-bottom: 20rpx;
}

.error {
  color: var(--danger);
  font-size: 24rpx;
  margin-top: 12rpx;
}
