# 后续产品实施计划

既有设计已确认，用户明确要求继续实现完整本地产品与微信工程。按稳定契约并行实现互不写入的目录，控制代理完成服务端与集成，最后独立审查。无需再停在文档阶段。

- [ ] A：公平模块+机器人，目录packages/fairness与packages/bot；固定种子/拒绝采样/篡改/视图约束测试。
- [ ] B：浏览器可玩界面，目录apps/web；按contract.md串接登录、大厅、房间、牌桌、规则、核验，响应式与错误/重连反馈。
- [ ] C：原生微信工程，目录apps/wechat；同一HTTP/WS协议，微信登录/本地游客模式、分享path、生命周期、开发导入说明与静态测试。
- [ ] D：服务端与存储，目录apps/server；先写集成断言验证guest认证、创建/加入/准备/机器人/贡献、越权和幂等，再实现HTTP/WS和持久化恢复；本地文件AES-GCM加密、生产Postgres，生产禁用模拟身份。
- [ ] E：一键启动、部署文件、完整测试与浏览器交互验收，根README启动说明。统一检查npm test/typecheck/exhaustive及服务端集成。修复具体失败后回归。
- [ ] F：独立审查整个新增产品，重点房间身份、秘密泄漏、随机承诺、客户端实际可操作；修复后复验，交付可访问本地URL及微信导入目录。

状态/接口/默认数值/错误语义以contract.md为准。外部账号未提供不阻塞本地实现，但微信真实登录/群分享/发布只能在账号配置后验证，不以浏览器测试代替。
