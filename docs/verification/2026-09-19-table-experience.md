# 牌桌体验与联机验证（2026-09-19）

## 基线与工作区

- 执行 `git fetch origin`；开始时 `main` 与 `origin/main` 都是 `fa5bc43`，左右提交差为 `0 0`。
- 修改位于 `codex/table-experience` 分支，尚未提交、推送或部署。
- 原有未跟踪的 `.DS_Store`、`docs/.DS_Store`、`apps/server/test/holding.test.ts` 保留不动。

## 本次改动

- 网页和小程序增加独立语音/音效开关，音乐入口保留；5 条中文行动短语音，4 种发牌/筹码/轻触/结算音效，两端包内 MP3 相同。
- 基于事件 seq 去重；初始/重连快照仅建立基线；静音与后台状态仍推进基线，避免打开声音后补播历史。批量追赶只保留最新行动的短语音；行动与翻牌同帧不会漏掉语音。
- 网页刷新后已开启的偏好显示“待启用”，需点击对应按钮解锁当前页面声音。切后台停止音频，返回后同步新局面。
- 浅绿桌面层次、当前行动者、下注金额、牌背纹理、按钮反馈；网页按状态差量播放发牌/下注/结算动画，并尊重减少动态效果设置。取消整个操作面板的底部吸附，避免窄屏遮住自己的底牌。
- 小程序加入相同配色层次、事件驱动的发牌/底池反馈与声音控制。
- 新增 `docs/local-multiplayer.md` 与音频重建脚本 `scripts/generate-audio.py`。

## 实际执行结果

- `npm run smoke`：41/41 通过。真实临时 Node 服务端，完整开局、行动、结算、核验及 WebSocket 关闭码流程。
- `node --test apps/server/test/ws.test.ts`：10/10 通过。多身份广播、底牌隔离、断线重连、心跳及顶号。
- `npm test`：531 项中 530 通过，1 失败。唯一失败是开始前就存在的未跟踪文件 `apps/server/test/holding.test.ts`，它引用了不存在的 `../src/rooms/holding.ts`。
- `npm run typecheck`：同样被上述文件的 TS2307 阻塞。使用仓库外临时 tsconfig 继承本项目配置，仅排除此文件并指定本项目 `node_modules/@types` 后，类型检查通过；没有修改项目默认检查范围。
- `git diff --check`：通过。
- 新增跨两端事件反馈行为测试：历史/重复快照静音、独立开关、后台处理、重连基线、批量事件、行动与翻牌同帧。
- 独立代码审查发现并修复回前台首条行动被跳过的问题；新增无后台事件恢复、旧同步请求不得恢复后台音频两个生命周期测试，复查通过。
- HTTP 静态资源测试验证 MP3 返回 `audio/mpeg` 与非空内容。

## 浏览器实测

本地服务端监听 `127.0.0.1:8788`，测试数据隔离到 `/tmp/poker-ui-review-data`。

- 从大厅创建 1 真人 + 3 机器人房间，准备、开赛、点击跟注，确认底池与行动者更新。
- 检查桌面及 390 × 844 手机视口；底牌、桌面、声音按钮均可见，操作区域不再覆盖底牌。
- 开启语音与音效，浏览器 audio 元素的 readyState 为 4，无 MediaError；预览 `check.mp3` 与 `chips.mp3` 播放进度推进。
- 机器人真实跟注后，语音通道切换为 `call.mp3` 且播放到末尾；音效通道播放 `chips.mp3`。
- 刷新后显示“待启用”，点击恢复；测试结束恢复默认浏览器视口。

## 未验证与后续部署条件

- 没有连接两台物理设备；多用户测试通过真实 HTTP/WebSocket 客户端完成。
- 没有运行微信开发者工具或微信真机，尚不能确认真实微信登录、分享、音频播放、布局和前后台生命周期。
- 尚未部署公网服务。当前网页微信入口仍需手动输入 code；production 禁用游客后，还需完善网页正式登录流程。
- 多实例房间归属与广播未实现，Postgres 持久化不能代替它们；以单进程部署作为当前基线。
