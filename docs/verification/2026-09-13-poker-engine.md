# Task 6 实施与验证报告

状态：六项任务实现及逐项代码审查完成，整体审查另见后续记录。未修改生产源码；未实现服务器或微信。

## 覆盖

- 独立 `referenceSeven` 基于七张牌频数、点数集合及花色集合，直接判定最佳向量；不调用生产 evaluate/compare/内部函数。
- 10,000 固定种子七张样本对照；测试专用 LCG 与 Fisher–Yates。
- 五张牌 2,598,960 种完整穷举，并断言标准 9 类计数及总数。
- 混合合法行动及优先持续全押，各覆盖 2～9 人各 20 场；每次转换检查守恒、唯一牌、合法 actor、整数非负及旧状态未变；自然终局且奖金正确。100,000 转换上限失败而非强行结算。
- 公共 API 固定牌序开发演示及 README 可信边界、规则和阶段二接入要求。
- betting.test.ts 展开原压缩测试为逐行 setup/action/assert；不增加 formatter 依赖。view.test.ts 保存原 hole 数组内容并在篡改返回视图后直接断言原数组内容未变。

## RED / GREEN

本任务为既有已通过规则代码添加独立验证，新增测试首次运行即 62/62 GREEN，未发现生产 bug，因此没有宣称不存在的生产 RED 或修复循环。未修改任何生产实现。一次尝试使用已安装 TypeScript 编译器 printer 格式化测试时，该版本不提供对应 JS API，工具调用失败；改用本地文本编辑，未引入依赖，最终 typecheck 和测试均通过。

## 最终实际验证

| 命令 | 结果 | 实测墙钟耗时 |
|---|---|---:|
| `npm ci` | 退出码 0 | 1.614 秒 |
| `npm test` | 退出码 0 | 2.630 秒 |
| `npm run typecheck` | 退出码 0 | 1.875 秒 |
| `npm run test:exhaustive` | 退出码 0 | 3.863 秒 |
| `npm run demo` | 退出码 0 | 0.372 秒 |

| 模式 | 人数 | 完整比赛 | 手牌数 | 行动数 |
|---|---:|---:|---:|---:|
| mixed | 2 | 20 | 1656 | 1677 |
| mixed | 3 | 20 | 1436 | 2252 |
| mixed | 4 | 20 | 1831 | 2129 |
| mixed | 5 | 20 | 1779 | 2273 |
| mixed | 6 | 20 | 1721 | 2089 |
| mixed | 7 | 20 | 1886 | 2535 |
| mixed | 8 | 20 | 1668 | 2297 |
| mixed | 9 | 20 | 1972 | 2435 |
| all-in | 2 | 20 | 20 | 40 |
| all-in | 3 | 20 | 24 | 69 |
| all-in | 4 | 20 | 21 | 82 |
| all-in | 5 | 20 | 22 | 104 |
| all-in | 6 | 20 | 23 | 126 |
| all-in | 7 | 20 | 24 | 148 |
| all-in | 8 | 20 | 24 | 168 |
| all-in | 9 | 20 | 21 | 182 |

普通测试 62/62，穷举测试 1/1；七张对照 10,000。穷举断言通过的计数：[1302540,1098240,123552,54912,10200,5108,3744,624,40]，总数 2,598,960。所有 320 场比赛自然终局；演示座位 0 于第 1 手获胜，共 3,000 筹码。耗时为 Python monotonic 包围命令测得的墙钟时间（含 npm/进程启动），非估算。

`git diff --check` 退出 0。对生产 src 搜索 Math.random、Date、fetch、node 网络/crypto、setTimeout/setInterval 无匹配（rg 无匹配退出 1）；源码仅使用本地模块导入。生产随机、时钟和网络仍由后续接入层负责。

## 局限与后续审查

七张验证是确定性抽样，并非全部七张牌穷举；模拟是有限轨迹测试。固定演示和测试随机数绝不可用于实际洗牌。父任务应审查独立性、逐转换守恒及私密视图复制；阶段二身份、持久化、公平协议和设备验收尚未实现。


## 最终整体审查与复验

六项实现均由独立子代理审查。整体审查发现并修复两项缺陷：短大盲已被小盲覆盖时仍要求行动，以及五张/七张稀疏牌数组绕过校验。修复提交 e521737；针对性复查确认两项均解决，无开放审查问题。

控制代理在 e521737 再次运行 npm test（67/67）、npm run typecheck、npm run test:exhaustive（全部 2,598,960 组合）、npm run demo 及 git diff --check，全部成功；工作目录干净。普通测试包含 10,000 组独立七张对照及 320 场完整比赛模拟。此结论限于阶段一规则引擎，不表示微信产品或安全随机发牌已经交付。

代码保留于 codex/poker-engine-implementation 隔离分支，未推送、未发布。
